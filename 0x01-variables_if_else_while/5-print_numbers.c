#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * Main - Print all single digit numbers
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	printf("%i", i);
	putchar('\n');

	return (0);
}
