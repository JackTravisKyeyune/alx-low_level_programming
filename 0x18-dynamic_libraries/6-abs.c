#include "main.h"
/**
 * _abs - Returns absolute value ofgiven number
 * @n: input number as an integer
 * Return: Absolute value of n
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (n * -1);
}
